import styled from 'styled-components';

import { Icon, Highlight } from '$components';

export const BankItemStyle = styled(Highlight)`
  width: 100%;
  flex-direction: row;
  align-items: center;
`;

export const AngleIconStyled = styled(Icon)`
  margin-left: auto;
  margin-right: 16px;
`;
