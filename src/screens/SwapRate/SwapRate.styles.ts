import styled from 'styled-components';

import { Button } from '$components';

export const SwapRateContainerStyled = styled.View`
  flex: 1;
  padding-horizontal: 16px;
`;

export const SwapButtonStyled = styled(Button)`
  margin-top: auto;
  margin-bottom: 8px;
`;
