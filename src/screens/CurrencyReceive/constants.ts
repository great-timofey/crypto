import { DEVICE_HEIGHT, mrIX } from '$global/device';

export const CURRENCY_RECEIVE_BLOCKCHAIN_ICON_WIDTH = 24;
export const QRCODE_SIZE = 224;
export const QRCODE_CONTAINER_SIZE = 256;
export const CURRENCY_RECEIVE_BOTTOMSHEET_WARNING_HEIGHT =
  DEVICE_HEIGHT * (mrIX ? 0.6 : 0.7);
