import styled from 'styled-components';

import { Typography } from '$components';

export const CurrencySendResumeStyled = styled.ScrollView`
  flex: 1;
  padding: 16px;
`;

export const CurrencySendStepTitleStyled = styled(Typography)`
  margin-bottom: 24px;
`;
