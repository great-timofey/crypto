import { defineMessages } from 'react-intl';

const messages = {
  noConnection: {
    id: 'noConnection',
    defaultMessage: `No network connection is active. Please check your connection`,
  },
};

export default defineMessages(messages);
